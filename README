1. cd dataset_creation ## Follow the README in the directory
2. cd ../disentanglement_hyper_selection ## Follow the README in the directory
3. cd ../udr_calculation ## Follow the README in the directory
4. cd ../disentanglement_post_model_selection ## Follow the README in the directory
